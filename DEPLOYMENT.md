# Egypt Unfolded deployment map

- GitHub Pages publishes the repository root.
- Cloudflare Pages publishes `cloudflare/`.
- Netlify publishes `netlify/`.

Each production domain has original articles and self-referencing canonical URLs.
